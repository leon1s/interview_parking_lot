/**
 * @description a car model, information of a car
 */

class Car {
    constructor (number) {
        this.number = number; // unique property of an instance of car class
    }
}

module.exports = Car;
